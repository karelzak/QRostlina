End-users do not want to keep track about individual plants and print uniq 
QR codes for each plan (B-xxx).

We need to modify the application to keep track about species on beds (what species is
used at the bed's cell), but do not need individual plants with IDs.

It means:
 * location (bed / crate) cell needs to point to species
 * we need per-species list of locations where it's used
 * remove plant screen at all
 * remove plant entity from database
 * improve species screen to list location (two tabs -- beds, creter)
 * when add a new plant to bed then just select what specie

 * move plants between crate and bed can be simplified, just one button
   make whole bed or crate empty (remove all reference for the location
   from species)

 * if individual plant died then user select location in bed grid and
   remove it (application remove the cell from species)


